name: watchy_sync
description: Watchy fitness sync app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  
  # BLE
  flutter_blue_plus: ^1.31.0
  
  # Firebase
  firebase_core: ^2.24.0
  firebase_database: ^10.4.0
  
  # Permisos
  permission_handler: ^11.0.1
  path_provider: ^2.1.0
  audioplayers: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true